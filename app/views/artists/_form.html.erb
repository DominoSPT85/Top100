<h1><%= @artist.id ? 'Edit Artist' : 'Add new Artist' %></h1>

<%= form_for @artist do |a| %>
  <%= a.label :name %>
  <%= a.text_field :name, required: true, autofocus: true %>

  <%= s.submit %>

<% end %>